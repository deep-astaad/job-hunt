apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: job-hunt
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`job-hunt.neovara.uk`)
      kind: Rule
      services:
        - name: frontend
          port: 80
